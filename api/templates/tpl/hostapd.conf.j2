{{ config_notice }}

{# Logging -#}
logger_stdout=-1
logger_stdout_level=2

{# Interface and wlan options -#}
ignore_broadcast_ssid=0
ssid={{ network.wlan_ssid }}
interface={{ network.wlan_interface }}
# ctrl_interface=/var/run/hostapd
channel={{ network.wlan_channel }}

{# Device configuration -#}
{{ network.wlan_hostapd_config }}
